# Signed PDFs Directory

This directory stores signed PDF files generated by the application.

Files are automatically created with the naming pattern: `signed-{timestamp}.pdf`

**Note:** This directory is excluded from git (see .gitignore)
